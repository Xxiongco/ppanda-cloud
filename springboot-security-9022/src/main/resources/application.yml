server:
  port: 9022
  servlet:
    context-path: /panda-9022

feign:
  # 开启这个，才能使用fallback属性
#  hystrix:
#    enabled: true
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic

spring:
  security:
    user:
      name: root
      password: root

